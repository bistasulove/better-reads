<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Edit book - <%= @book.title %></h1>

  <%= render "form", book: @book %>

  <%= link_to "Cancel", @book, class: "ml-2 rounded-lg py-3 px-5 bg-gray-200 inline-block font-medium" %>
</div>
